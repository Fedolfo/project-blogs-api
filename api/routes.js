const { addUser, addLogin, allUsers, findByIdUser,
  addCategory, allCategories } = require('../controllers/index');

module.exports = {
  addUser,
  addLogin,
  allUsers,
  findByIdUser,
  addCategory,
  allCategories,
};