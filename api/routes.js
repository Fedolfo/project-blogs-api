const { addUser, addLogin, allUsers, findByIdUser,
  addCategory, allCategories, createPostCategory,
  allPosts, findByIdPost } = require('../controllers/index');

module.exports = {
  addUser,
  addLogin,
  allUsers,
  findByIdUser,
  addCategory,
  allCategories,
  createPostCategory,
  allPosts,
  findByIdPost,
};