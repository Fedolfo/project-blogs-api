const { addUser, addLogin, allUsers, findByIdUser } = require('./user');
const { addCategory } = require('./category');

module.exports = {
  addUser,
  addLogin,
  allUsers,
  findByIdUser,
  addCategory,
};